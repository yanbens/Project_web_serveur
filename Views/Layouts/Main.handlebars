<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        {{#each styles}}
            <link rel="stylesheet" href={{this}} />
        {{/each}}

        {{#each scripts}}
            <script src={{this}} type="module"></script>
        {{/each}}

        <title>{{titre}}</title>
    </head>
    <body>
        
        <main>
            {{{body}}}
        </main>
        <footer>&copy; Yanis bensalem</footer>
    </body>
</html>