<div class="container">
    <h2 class="text-center">Liste des tâches</h2>

    {{#if user}}
        <p>Bonjour, {{user.nom}} 👋</p>
        <form action="/deconnexion" method="POST">
            <button type="submit" class="btn-delete">Se déconnecter</button>
        </form>
    {{else}}
        <p class="text-center">
            <a href="/connexion">Se connecter</a> |
            <a href="/inscription">S’inscrire</a>
        </p>
    {{/if}}

    <ul class="tasks">
        {{#each taches}}
        <li class="{{#if this.pinned}}pinned{{/if}}">
            <h3>{{this.title}}</h3>
            <p>Description: {{this.description}}</p>
            <p>Priorité: <strong>{{this.priority}}</strong></p>
            <p>Échéance: {{this.due_date}}</p>
            <p>Épinglée: {{#if this.pinned}}✅ Oui{{else}}❌ Non{{/if}}</p>

            <a href="/edit/{{this.id}}" class="btn-edit">Modifier</a>

            <form action="/api/taches/{{this.id}}?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="btn-delete">Supprimer</button>
            </form>
        </li>
        {{/each}}
    </ul>

    <a href="/creer_Tache" class="btn-add">Ajouter une tâche</a>
</div>
